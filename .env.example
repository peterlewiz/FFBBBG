# Copy this file to .env.local and fill in your own Supabase project's
# values (Project Settings → API in the Supabase dashboard). Required for
# the Predictions pick'em page; the rest of the site works without it.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
